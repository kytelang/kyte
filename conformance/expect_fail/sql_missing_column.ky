// EXPECT-FAIL: codegen
// The SELECT omits a column for the plain field `name` (typo `naem`), so the
// compiler must reject this query at build time.
import data.db;
import serde.json;
import serde.source;
import list;
@serializable struct Product { pub id: int, pub name: string }
async fn load(conn: db.Connection): void {
    let p = list.List<db.DbValue>();
    let _ = await db.query<Product>(conn, "SELECT id, naem FROM products", p);
}
fn main(): void {}