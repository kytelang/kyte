// EXPECT-FAIL: typecheck
import assert;
fn need(s: string): int { return 1; }
fn get(b: bool): string | undefined { if (b) { return "x"; } return undefined; }
@test
fn optional_to_plain_arg_is_rejected(): void {
    let x = get(false);
    let r = need(x);
    assert.isTrue(r > 0);
}