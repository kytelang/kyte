import assert;
import io.dir;

fn main(): int {
    let d = "kyte_ci_dir";
    dir.Dir.create(d, 511);
    assert.isTrue(dir.Dir.exists(d));
    assert.isTrue(dir.Dir.isDir(d));
    dir.Dir.remove(d);
    console.log("t03_dir ok");
    return 0;
}
