import scoped.widget_a;
import scoped.widget_b;
import assert;

@test
fn test_each_module_resolves_its_own_widget(): void {
    assert.equalInt(widget_a.makeA(), 10);
    assert.equalInt(widget_b.makeB(), 5);
}
