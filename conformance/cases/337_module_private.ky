import assert;

struct S {
    x: int,
    init(v: int) { self.x = v; }
    fn secret(self: S): int { return self.x * 2; }
}

@test
fn same_module_access_to_non_pub_members(): void {
    let s = S(21);
    assert.equalInt(s.x, 21);
    assert.equalInt(s.secret(), 42);
}
