@serializable
pub struct Register {
    pub email: string,
    pub password: string,
    pub name: string,
    pub role: string,
    init() { self.email = ""; self.password = ""; self.name = ""; self.role = "customer"; }
}
